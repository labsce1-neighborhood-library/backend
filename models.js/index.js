/*This file will be feature all of the models to make exporting more organizinged  import model and then export them all out */

const booksDB = require("./booksDB");


/* Add in every import to the export */
module.exports = {
    booksDB,
}